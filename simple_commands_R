#intsall and activate the required packages
if (!require("pacman")) install.packages("pacman")
pacman::p_load(pacman, rio) 

#import the csv-data
csv <- import("C:/Users/pschlief/Desktop/data.cscv")

#show minimun
min(data$row)
#show maximum
max(data$row)
#show median
median(data$row)

#save file
save (mtcars, file="C:/Users/pschlief/Desktop/test.RData")
